# JDK 21 required for compilation (system JDK is 26, incompatible with Lombok).
# Set JAVA_HOME before running ./gradlew:
#   export JAVA_HOME=$HOME/.jdks/ms-21.0.11
# Or inline:
#   JAVA_HOME=$HOME/.jdks/ms-21.0.11 ./gradlew test
